# 乗レコ TODO

新セッションでは、まず本ファイルを読んで次の着手項目を選ぶ。
詳しい仕様や経緯は `HANDOFF.md` §12〜§14、ビジネス背景は [Notion 開発ノート](https://www.notion.so/35b71b458b63818494afe7c1ab917ca5)。

---

## 🔥 最優先（プロダクトとして欠けている）

- [ ] **記録モード認証グラデーション整備** ← キャラ自動獲得の前提
  - データモデル拡張（Notion §「記録モード設計」既定スキーマに合わせる）:
    - `source`: `manual` / `gps_button` / `ic_card`
    - `verified`: bool
    - `stop_type`: `alighted` / `passed` / `boarded`
    - `date_precision`: `day` / `month` / `year` / `unknown`
    - `gps_lat` / `gps_lon`, `rode_at`, `recorded_at`
  - 既存 trip には後方互換 default を当てる（`source: manual`, `verified: false`, `stop_type: alighted`, `date_precision: day`）
  - 地図画面 📝 = `source: manual` で保存
  - ログ画面 GPS = `source: gps_button, verified: true` で保存
  - 認証バッジ ⚪🟢✅⭐ を trip 一覧・モーダルに表示
  - **キャラ自動獲得は `verified === true` のみ**（manual は地図塗り・統計のみ反映）
  - 後追い登録（自己申告→GPS/IC で昇格）→ 昇格時に再判定して獲得可能に

- [ ] **Supabase 認証＋ログインページ**
  - 現状：trip データは端末＋Supabase共有、誰の記録か区別なし
  - メール or Google ログイン → user_id をデータに紐付け

- [ ] **マイページ**
  - 自分の trip 一覧／公式完乗率（verified のみ）／全記録完乗率（manual 含む）／削除・編集
  - 既存 trip にも user_id を遡及付与
  - 後追い登録 UI（trip ごとに「GPS で認証する」「IC履歴で認証する」）

- [ ] **シェア機能（OGP 画像生成）**
  - 自分の達成地図のスクショ → Twitter/X で拡散
  - 主要ターミナル何駅制覇など要約も載せる
  - **認証済みのみ**シェア可（Notion 方針 - コア化への自然誘導）

## 🟡 体験向上（コア層の継続率を上げる）

- [x] ~~**時間軸フィルター**~~ → **✅ 完了** (v66)
  - 地図上部中央にピル `📅 [全期間][今年][去年][カスタム]`
  - `trip.date` でフィルタ、`localStorage.norireco_date_filter` に永続化

- [x] ~~**駅 UI の個人化（基本部分）**~~ → **✅ 完了** (v74〜v75)
  - 訪問回数で育つマーカー（Lv1: 1-4回 / Lv2: 5-9 / Lv3: 10-49 / Lv4: 50+）
  - 金色リング・ハロー（パルスアニメ）で達成感
  - `slVisitCount[駅名]` を `rebuildRiddenStations` で集計

- [ ] **`stop_type` 反映の駅UI個人化**
  - 「降りた駅 (alighted) = ●大」/「乗車のみ (boarded) = ◎中」/「通過 (passed) = ○小」
  - Notion §「乗降駅レイヤー」参照
  - 記録モード認証グラデーションが先（stop_type フィールドが必要）

- [x] ~~**駅キャラ・モーダル・期間限定フレームワーク**~~ → **✅ 完了** (v76〜v88)
  - SVG キャラを mergedStations に紐付け、マーカー本体を置換
  - 駅クリックでモーダル拡大、キャラ詳細・乗り入れ系統表示
  - 1駅複数キャラの選択 UI（localStorage 永続化）
  - 期間限定キャラ（rarity / default_unlocked / available_from-until / obtainable_at）
  - 所持 / 🔒未獲得 セクション分離
  - 🎭 ボタンで表示 ON/OFF
  - 現在のキャラ: クワテン・ヨウマユ・ヨマツリマユ (期間限定)・アール・プレーン 2.0・コミヤウ

- [ ] **キャラ自動獲得（Phase 2）**
  - trip 保存時に obtainable_at 該当駅を訪問していれば自動 grant
  - **前提: 記録モード認証グラデーション** (verified gate)
  - トースト通知「🎉 キャラ獲得！」
  - 未獲得駅に ✨ オーバーレイ（取りに行こう演出）

- [ ] **区間記録モード（📝）の拡張**
  - 経路プレビュー線（選択中の駅間を色付き線で予告）
  - 保存後の trip 一覧・削除 UI（記録モード内で完結）
  - 駅長押しで詳細パネル（駅情報・乗り入れ路線・過去訪問数）

- [ ] **キャラ図鑑タブ**
  - 統計タブに「所持キャラ一覧」「全キャラ一覧（未獲得は ?）」
  - 期間限定キャラの取得期限カウントダウン
  - 獲得日時記録

## 🟢 データ充実

- [ ] **直通系統の追加**（service_lines_master.json）
  - F ライナー（元町中華街〜小川町/小手指）
  - 半蔵門線↔田園都市線↔東武スカイツリーライン
  - 東急目黒線↔南北線/三田線↔埼玉高速
  - 千代田線↔小田急小田原線/常磐線各停

- [ ] **新幹線各系統の手動連結**
  - 東海道・山陽新幹線（東京〜博多）を1系統に
  - 北陸新幹線（東京〜敦賀）、九州新幹線、西九州新幹線
  - 東北新幹線↔北海道新幹線（東京〜新函館北斗）
  - 山形新幹線、秋田新幹線

- [ ] **駅キャラを増やす**
  - 主要ターミナル（東京・新宿・渋谷・池袋・横浜・大阪・名古屋・札幌・仙台・博多）
  - 地域文化を反映したデザイン（クワテン式：地元産業 × 神話/伝統 × ご当地）
  - ファイル形式: SVG（viewBox 64x64, 1-5KB目標）

- [x] ~~**N02 inline-支線パターン洗い出し**~~ → **✅ 完了** (4件修正、コミット `4463fc2`)

## 🔧 パフォーマンス・UI

- [x] ~~**地図描画を営業系統ベースに**~~ → **✅ 完了** (Phase 2, v70)
  - 638系統 × 2層描画 → 単層描画でパフォーマンス改善
  - branch/o 論理撤廃、`drawServiceLineBase` + `drawStationsLayer` の単純構造
  - 🚆 トグル撤去（常時ベース化）

- [x] ~~**右下 FAB スタック整列**~~ → **✅ 完了** (v71)
- [x] ~~**ズームボタンを右下に統合**~~ → **✅ 完了** (v72)
- [x] ~~**デプロイ状態バッジ（v○○ 🟢/🔄）**~~ → **✅ 完了** (v67)

- [ ] **路線一覧タブの絞り込み UI**
  - 638 系統あるとスクロール大変
  - 検索ボックス・地域フィルター・運営会社フィルター

- [ ] **モバイル描画パフォーマンス継続改善**
  - Phase 2 で改善したが、まだ重い駅がある（多系統駅のパイ・キャラ）
  - 必要なら viewport culling、divIcon → canvas への寄せを再検討

## 🎮 将来（コア層 → マス層フェーズ）

- [ ] **キャラのリアル画像版（モーダル内）**
  - SVG はマップ用、モーダル内は WebP/Lottie の高品質画像
  - `image_path` / `image_animation` フィールドを characters_master に追加
  - 「お、こんなクオリティ！」の演出

- [ ] **マップ上の広告アイコン**
  - 別レイヤー (`adsLayer`) でスポンサー駅アイコン表示
  - `ads_master.json` 新規（id, sponsor, station_ids, svg_path, link_url, period_from-to）
  - クリックでモーダル → 詳細＋リンク
  - impression / click 集計、Supabase 集約
  - プレミアム会員は非表示（収益モデル整合）
  - 3万人規模以降に本格展開（自治体・鉄道会社連携）

- [ ] **キャラの色変更 UI**
  - SVG 内の塗りを `currentColor` / CSS変数 に変更
  - characters_master に `colorable_parts` 追加
  - モーダルでカラースウォッチから選択

- [ ] **キャラの装束違い（variants）**
  - 1キャラに複数 SVG variant パスを `variants: [{id, name, svg_path}]` で持たせる
  - 桜・夏・冬・祭装束など季節バリエーション

- [ ] **ゲーミフィケーション・イベント機能**
  - スタンプラリー × 乗車記録（自治体連携・季節イベント・廃線記念）
  - イベントマスター `events_master.json`（id, period, target_stations, reward_badge）
  - 3万人規模になってから本格展開（設計のみ先行）

- [ ] **アプリ化（React Native or Flutter）**
  - AdMob 導入で広告単価 2〜5倍
  - GPS常時取得、プッシュ通知（押し忘れ防止）
  - ユーザー数 5,000人を超えたら検討

---

## メモ

- **main 直 push 運用**（個人開発、PR・専用ブランチ不要、自動承認設定済み）
- 編集後は **`sw.js` の `CACHE_VERSION` を上げる**こと（現在 v88）
- HTML 編集後は `</script></body></html>` が末尾に残っているか必ず確認
- 新規 trip の `lineId` は `service_lines_master.json` の id を使う（旧 N02 id も `LEGACY_LINE_ID_ALIAS` で透過解決）
- キャラ追加は: `characters/<id>.svg` 配置 → `characters_master.json` に entry 追加 → `sw.js` STATIC_ASSETS に追加 → CACHE_VERSION 上げ

## 着手済み（参考、`HANDOFF.md` §12〜§14 詳細）

### Phase 1（〜v60）
- ✅ 営業系統マスター（637系統・10,450駅）
- ✅ 営業系統オーバーレイ（🚆 ボタン）→ Phase 2 で撤去
- ✅ 同名近接駅統合・パイチャート（運行系統色）
- ✅ 路線一覧・統計タブの営業系統化
- ✅ ログ画面の営業系統セレクト
- ✅ 区間記録モード（📝 ボタン、複数駅クリック→自動連結→乗換対応→Supabase 保存）
- ✅ ツールチップの遅延評価（運行系統名表示）
- ✅ 路線名・支線名の正規化（本線、京急/阪急/メトロ等 prefix）

### Phase 2（v60〜v70）
- ✅ 期間フィルタピル（v66）
- ✅ バージョンバッジ（v67）：両画面のヘッダーに `v○○ 🟢/🔄` 表示
- ✅ ログ画面にも SW 登録＋バッジ（v69）
- ✅ 地図描画を営業系統ベースに刷新（v70）：`slRiddenSt` + `drawServiceLineBase` + `drawStationsLayer`、N02 はフォールバックのみ
- ✅ FAB 整列・ズームボタン右下統合（v71-v72）

### Phase 2.5（v74〜v88）— 駅キャラシステム
- ✅ 訪問回数で育つマーカー（Lv1-4）、金色リング・ハロー（v74-v75）
- ✅ characters_master.json + SVG キャラ読込（v76）
- ✅ キャラ ON/OFF トグル 🎭（v77）
- ✅ キャラがマーカー本体を置換、外周に細い路線色リング（v78）
- ✅ 駅クリックでキャラ詳細モーダル（v82-v84）
- ✅ モーダル内キャラ切替 UI、localStorage 永続化（v85）
- ✅ 期間限定・所持/未獲得・レアリティバッジ枠組み（v87）
- ✅ 現在のキャラ: クワテン (八王子)、ヨウマユ (八王子)、ヨマツリマユ (八王子・期間限定)、アール・プレーン 2.0 (立川)、コミヤウ (小宮)
